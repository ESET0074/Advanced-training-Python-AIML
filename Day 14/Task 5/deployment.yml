apiVersion: v1
kind: Pod
metadata:
  name: my-app-pod
  labels:
    app: my-app
spec:
  containers:
    - name: my-app-container
      image: docker.io/eset0074/get-sha:latest
      ports:
        - containerPort: 5000
      imagePullPolicy: Always
  restartPolicy: OnFailure

